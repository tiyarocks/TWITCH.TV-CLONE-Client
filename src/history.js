//createHistory:it is a func we can call to create a history object that we can get access to anywhere in our project
import createHistory from "history/createBrowserHistory"
export default createHistory()